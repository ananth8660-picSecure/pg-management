rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Binary files are intentionally disabled in Firebase Storage.
    // All photos/PDF/docs are AES-GCM encrypted and stored in Cloudflare R2.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
