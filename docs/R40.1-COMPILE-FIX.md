# R40.1 Compile Fix

Fixed the compile failures reported from the R40 source:

- `src/styles.scss`: removed literal `\n` escape text that had been written into SCSS instead of real line breaks.
- `operations.page.ts`: rebuilt the dynamic form control-flow so `<label>` is closed outside the Angular `@if/@else` blocks.
- `operations.page.ts`: explicitly typed the completed/read row as `Row` and normalized ID comparison, fixing TS7053.
- Rechecked Operations template tag balance after the repair.

No Firebase / R2 configuration change is required for this compile-only patch.
