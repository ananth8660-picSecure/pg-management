# R31.1 Login building SVG fix

- Fixed malformed SVG XML that caused the browser to display the image alt text instead of the building artwork.
- Validated the SVG with an XML parser.
- Enlarged the Paying Guest building hero and adjusted responsive proportions.
