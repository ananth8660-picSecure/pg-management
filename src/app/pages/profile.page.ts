import { Component, OnInit, signal } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { RouterLink } from '@angular/router';
import { AuthService } from '../core/auth.service';
import { StoreService } from '../core/store.service';
import { ManagedUser, PagePermission, Role } from '../core/models';
import { APP_CONFIG } from '../config/app-config';
import { NotificationService } from '../core/notification.service';

@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`
<div class="page-head"><div><p class="eyebrow">{{auth.isOwner()?'PROFILE • ACCOUNT • ACCESS CENTER':'MY PROFILE • SECURITY • ACCESS'}}</p><h1>{{auth.isOwner()?'Profile & User Access':'My Profile'}}</h1><p>{{auth.isOwner()?'Manage your account, PG ownership, Managers, permissions and security.':'Your own account, security and the modules assigned to you by a PG Owner.'}}</p></div>@if(auth.isOwner()){<button class="primary-btn" (click)="openCreateUser()">+ Add Owner / Manager</button>}</div>
<section class="profile-access-hero panel"><div class="identity-card"><span class="profile-orb">{{initials}}</span><div><p class="eyebrow">SIGNED IN ACCOUNT</p><h2>{{auth.user()?.name}}</h2><span>{{auth.user()?.email}}</span><div class="identity-chips"><b>{{auth.user()?.role==='owner'?'Owner · Full PG Control':'Manager · Assigned Access'}}</b>@if(auth.isPlatformOwner()){<em>Creator</em>}<em>{{auth.tenant()?.name}}</em></div></div></div><div class="vault-card"><span>ACCOUNT SECURITY</span><b>{{auth.user()?.role==='owner'?'Owner Protected':'Manager Protected'}}</b><small>Firebase identity, tenant isolation, private encrypted file access and automatic session locking protect this account.</small><button class="secondary-btn compact own-reset-btn" type="button" [disabled]="ownResetBusy()" (click)="sendOwnPasswordReset()">{{ownResetBusy()?'Sending…':'Send password reset email'}}</button></div></section>
@if(auth.isOwner()){<section class="panel pg-identity-panel"><div class="panel-head"><div><p class="eyebrow">PG IDENTITY & LOCATION</p><h2>PG name and location</h2><p>Update the PG name and location used across this workspace. Changes sync to Firebase and update the app automatically.</p></div><span class="secure-pill">Owner Only</span></div><div class="pg-identity-grid"><label class="premium-field-card"><span>PG full name</span><small>Shown across the workspace and ownership areas.</small><input [(ngModel)]="pgIdentity.name" maxlength="80" autocomplete="organization" placeholder="Example: Sri Sai Men's PG"></label><label class="premium-field-card"><span>Short display name</span><small>Used in the sidebar where space is limited.</small><input [(ngModel)]="pgIdentity.shortName" maxlength="32" placeholder="Example: Sri Sai PG"></label><label class="premium-field-card span-2"><span>Location / City</span><small>Shown below the PG name so staff can identify the property location quickly.</small><input [(ngModel)]="pgIdentity.city" maxlength="80" autocomplete="address-level2" placeholder="Example: Vijayawada"></label></div>@if(pgIdentityError()){<div class="notice modal-error-top">{{pgIdentityError()}}</div>}@if(pgIdentityMessage()){<div class="ops-message">{{pgIdentityMessage()}}</div>}<div class="pg-identity-actions"><button class="secondary-btn" type="button" [disabled]="pgIdentitySaving()" (click)="resetPgIdentity()">Reset</button><button class="primary-btn" type="button" [attr.aria-busy]="pgIdentitySaving()" [disabled]="pgIdentitySaving()" (click)="savePgIdentity()">{{pgIdentitySaving()?'Saving…':'Save PG details'}}</button></div></section>}

@if(auth.isPlatformOwner()){<section class="panel super-owner-entry"><div class="super-owner-icon">SO</div><div class="super-owner-copy"><p class="eyebrow">CREATOR PLATFORM CONTROL</p><h2>Super Owner Customer Directory</h2><p>See every sold/customer PG, who owns it, how many users are active, and control customer access, support mode, suspension and forced sign-out.</p><div class="super-owner-pills"><span>Customer PGs</span><span>Access Control</span><span>Audit Protected</span></div></div><a routerLink="/platform" class="super-owner-open-btn">Open Locked Vault <span>→</span></a></section>}
<section class="panel push-access-panel"><div class="panel-head"><div><p class="eyebrow">PUSH NOTIFICATIONS</p><h2>Rent, Change & Security Alerts</h2><p>Receive rent due/overdue alerts, Owner/Manager change updates and Super Owner platform changes for this PG.</p></div><span class="secure-pill" [class.push-enabled]="notifications.state()==='enabled'">{{pushLabel}}</span></div><div class="push-access-grid"><article><b>Rent alerts</b><small>Due today and overdue rent reminders.</small></article><article><b>PG change alerts</b><small>Important changes made by another Owner or Manager.</small></article><article><b>Platform alerts</b><small>Super Owner access/suspension/sign-out updates sent to PG Owners.</small></article><article><b>Browser push</b><small>{{notifications.stateMessage()}}</small></article></div><div class="push-actions"><button class="primary-btn" type="button" [disabled]="pushBusy()||notifications.state()==='enabled'||notifications.state()==='unsupported'||notifications.state()==='not-configured'" (click)="enablePush()">{{pushBusy()?'Enabling…':notifications.state()==='enabled'?'Push Enabled':'Enable Push on This Device'}}</button><button class="secondary-btn" type="button" (click)="notifications.refreshSupportState()">Check Status</button></div></section>

@if(auth.isOwner()){<section class="panel sharing-rates-panel"><div class="panel-head"><div><p class="eyebrow">SHARING STARTING RATES</p><h2>Standard monthly rent by sharing</h2><p>Room sharing types are detected automatically from your Firebase room inventory. Add a new sharing type in Room Setup and it appears here when Profile is opened or refreshed. These are guidance rates; the final agreed tenant rent is captured separately at check-in.</p></div><span class="secure-pill">Owner Only</span></div><div class="sharing-rate-grid">@for(row of sharingRateDraft;track row.sharing){<label class="sharing-rate-card"><div class="sharing-rate-card-head"><span><b>{{row.sharing}} Sharing</b><small>Standard monthly starting rate</small></span><em>{{sharingVacancyCount(row.sharing)}} vacant beds</em></div><div class="rate-input premium-money-field"><span class="currency-prefix">₹</span><input type="number" min="0" step="100" inputmode="numeric" [(ngModel)]="row.amount" [placeholder]="'Enter '+row.sharing+' sharing rate'" [attr.aria-label]="row.sharing+' sharing starting rent'"></div><small class="rate-help">Shown in Vacancy, Room cards and New Tenant check-in. Owner can change this standard rate any time.</small></label>}@empty{<div class="premium-empty"><h3>No sharing types available yet</h3><p>Create the first room in Floor & Room Setup. Its sharing capacity will automatically become available here.</p></div>}</div>@if(sharingRatesError()){<div class="notice modal-error-top">{{sharingRatesError()}}</div>}@if(sharingRatesMessage()){<div class="ops-message">{{sharingRatesMessage()}}</div>}<div class="sharing-rate-actions"><button class="secondary-btn" type="button" [disabled]="sharingRatesSaving()" (click)="refreshSharingRateDraft()">Reset Unsaved Changes</button><button class="primary-btn" type="button" [attr.aria-busy]="sharingRatesSaving()" [disabled]="sharingRatesSaving()" (click)="saveSharingRates()">{{sharingRatesSaving()?'Saving rates…':'Save Starting Rates'}}</button></div></section>}

<section class="panel current-access-panel"><div class="panel-head"><div><p class="eyebrow">YOUR ACCESS</p><h2>{{auth.user()?.role==='owner'?'Owner Workspace Control':'Manager Workspace Access'}}</h2><p>{{auth.user()?.role==='owner'?'Full control inside this PG: property, residents, rent, users, branding, reports and audit.':'Only the modules assigned by an Owner are available in navigation and protected routes.'}}</p></div><span class="secure-pill">{{auth.user()?.role==='owner'?'Full PG Access':'Permission Based'}}</span></div><div class="access-facts"><article><b>{{auth.tenant()?.name}}</b><small>Active PG workspace</small></article><article><b>{{auth.user()?.role==='owner'?'All modules':(auth.user()?.permissions?.length||0)+' modules'}}</b><small>Accessible scope</small></article><article><b>R2 Encrypted</b><small>Private file vault</small></article><article><b>Audit Enabled</b><small>Name + role + time</small></article></div></section>
@if(!auth.isOwner()){<section class="panel manager-profile-panel"><div class="panel-head"><div><p class="eyebrow">MY ASSIGNED ACCESS</p><h2>Modules available to you</h2><p>Only these modules are visible in navigation and accepted by protected routes. Contact a PG Owner for changes.</p></div><span class="secure-pill">Owner Managed</span></div><div class="manager-permission-list">@for(p of managerVisiblePermissions();track p.key){<article><span>✓</span><div><b>{{p.label}}</b><small>{{p.desc}}</small></div></article>}@empty{<div class="premium-empty"><h3>No operational modules assigned</h3><p>Your Profile remains available. Ask a PG Owner to assign required access.</p></div>}</div></section>}
@if(message()){<div class="ops-message">{{message()}}</div>}@if(error()){<div class="notice">{{error()}}</div>}

@if(auth.isOwner()){
<section class="panel user-admin-panel"><div class="panel-head"><div><p class="eyebrow">PG OWNERSHIP & MANAGEMENT</p><h2>{{auth.tenant()?.name}} · Ownership & Manager Access</h2><p>This section belongs only to this PG. Add Co-owners, add Managers, change roles, enable/disable access and manage permissions. <b>Sensitive changes require your current password.</b></p></div><span class="secure-pill">Tenant Isolated</span></div><div class="account-flow four-step"><article><b>1 · Add Account</b><span>Email + temporary password</span></article><article><b>2 · Assign Role</b><span>Owner / Co-owner or Manager</span></article><article><b>3 · Set Access</b><span>Manager permissions</span></article><article><b>4 · Verify Change</b><span>Current Owner password required</span></article></div>
@if(loading()){<div class="premium-empty"><h3>Loading users…</h3></div>}@else{<div class="managed-user-grid">@for(u of users();track u.uid){<article class="managed-user-card" [class.disabled]="!u.active"><div class="managed-user-top"><span class="user-avatar">{{userInitials(u)}}</span><div><b>{{u.name}}</b><small>{{u.email}}</small></div><em [class.owner]="u.role==='owner'">{{u.role}}</em></div><div class="user-meta"><span>{{u.active?'Active':'Disabled'}}</span><span>{{u.role==='owner'?'All pages':u.permissions.length+' pages'}}</span></div><div class="user-card-actions"><button class="secondary-btn compact" (click)="openEdit(u)">Manage Access</button><button class="secondary-btn compact" (click)="resetPassword(u)">Reset Password</button>@if(u.uid!==auth.user()?.uid){<button class="secondary-btn compact force-logout-user" (click)="openForceLogout(u)">Force Logout</button>}</div></article>}</div>}
</section>}

@if(auth.isOwner()){<section class="panel change-panel"><div class="panel-head"><div><p class="eyebrow">ACCOUNTABILITY</p><h2>Recent Changes</h2></div><span class="secure-pill">Name + Role + Time</span></div><div class="audit-cards">@for(a of store.auditLogs().slice(0,12);track a.id){<article><span class="audit-role" [class.owner]="a.actorRole==='owner'">{{a.actorRole==='owner'?'OWNER':'MANAGER'}}</span><div><b>{{a.action}}</b><small>{{a.module}} · {{a.actorName}} · {{formatTime(a.createdAt)}}</small></div></article>}@empty{<div class="premium-empty"><h3>No changes recorded yet</h3></div>}</div></section>}

@if(editorOpen()){<div class="modal-backdrop" (click)="closeEditor()"><section class="form-modal modal-shell access-user-modal" (click)="$event.stopPropagation()"><div class="modal-header"><div><p class="eyebrow">{{editingUid?'EDIT ACCESS':'NEW ACCOUNT'}}</p><h2>{{editingUid?'Manage User':'Add Owner / Manager'}}</h2><p class="modal-subtitle">User is attached only to {{auth.tenant()?.name}}.</p></div><button class="modal-x" (click)="closeEditor()">×</button></div><div class="modal-body"><div class="form-grid"><label>Full name<input [(ngModel)]="draft.name"></label><label>Email<input [(ngModel)]="draft.email" type="email" [disabled]="!!editingUid"></label>@if(!editingUid){<label>Temporary password<div class="verify-password-field"><input [(ngModel)]="draft.password" [type]="showTemporaryPassword()?'text':'password'" autocomplete="new-password" placeholder="Minimum 8 characters"><button type="button" (click)="showTemporaryPassword.set(!showTemporaryPassword())">{{showTemporaryPassword()?'Hide':'Show'}}</button></div></label>}<label>Role<select [(ngModel)]="draft.role" (ngModelChange)="roleChanged($event)"><option value="manager">Manager · selected permissions</option><option value="owner">Owner / Co-owner · full PG access</option></select></label><label class="access-active"><input type="checkbox" [(ngModel)]="draft.active"> Account active</label></div>@if(draft.role==='manager'){<div class="access-permissions"><div class="floor-section-title"><div><h3>Manager permissions</h3><p>Only selected modules are visible and allowed.</p></div><button class="secondary-btn compact" (click)="recommended()">Recommended</button></div><div class="permission-grid">@for(p of permissions;track p.key){<label class="permission-card" [class.enabled]="draft.permissions.includes(p.key)"><span class="permission-switch"><input type="checkbox" [checked]="draft.permissions.includes(p.key)" (change)="togglePermission(p.key,$any($event.target).checked)"><i></i></span><div><b>{{p.label}}</b><small>{{p.desc}}</small></div></label>}</div></div>}@else{<div class="owner-access-note"><b>Owner / Co-owner = full PG access</b><span>Multiple Owners are supported inside the same PG. They can manage operations, branding and PG users, but cannot access other customer PGs or the Super Owner vault.</span></div>}<div class="owner-verify-box"><div><b>Confirm this access change</b><span>Enter your current Owner password. It is verified by Firebase and is never stored.</span></div><label>Current password<div class="verify-password-field"><input [(ngModel)]="verificationPassword" [type]="showVerificationPassword()?'text':'password'" autocomplete="current-password" placeholder="Enter your current password"><button type="button" (click)="showVerificationPassword.set(!showVerificationPassword())">{{showVerificationPassword()?'Hide':'Show'}}</button></div></label></div>@if(editorError()){<div class="notice">{{editorError()}}</div>}</div><div class="modal-footer"><button class="secondary-btn" (click)="closeEditor()">Cancel</button><button class="primary-btn" [disabled]="saving()" (click)="saveUser()">{{saving()?'Saving…':editingUid?'Save Access':'Create User'}}</button></div></section></div>}
@if(forceLogoutTarget()){<div class="modal-backdrop" (click)="closeForceLogout()"><section class="force-user-logout-modal" role="dialog" aria-modal="true" (click)="$event.stopPropagation()"><button class="modal-x" (click)="closeForceLogout()">×</button><div class="force-user-icon">↪</div><p class="eyebrow">SECURE USER SESSION CONTROL</p><h2>Force logout {{forceLogoutTarget()?.name}}?</h2><p>This ends the selected user's active PG session. They can sign in again with their valid email and password unless you also disable their access.</p><div class="force-user-summary"><span class="user-avatar">{{userInitials(forceLogoutTarget()!)}}</span><div><b>{{forceLogoutTarget()?.name}}</b><small>{{forceLogoutTarget()?.email}} · {{forceLogoutTarget()?.role}}</small></div></div><label>Current Owner password<div class="verify-password-field"><input [(ngModel)]="forceLogoutPassword" [type]="showForceLogoutPassword()?'text':'password'" autocomplete="current-password" placeholder="Verify your password"><button type="button" (click)="showForceLogoutPassword.set(!showForceLogoutPassword())">{{showForceLogoutPassword()?'Hide':'Show'}}</button></div></label>@if(forceLogoutError()){<div class="notice">{{forceLogoutError()}}</div>}<div class="force-user-actions"><button class="secondary-btn" (click)="closeForceLogout()">Cancel</button><button class="danger-user-btn" [disabled]="forceLogoutBusy()" (click)="confirmForceLogout()">{{forceLogoutBusy()?'Signing out…':'Force Logout User'}}</button></div></section></div>}

@if(resetTarget()){<div class="modal-backdrop" (click)="resetTarget.set(null)"><section class="force-user-logout-modal" role="dialog" aria-modal="true" (click)="$event.stopPropagation()"><button class="modal-x" type="button" (click)="resetTarget.set(null)">×</button><div class="force-user-icon">✉</div><p class="eyebrow">PASSWORD RECOVERY</p><h2>Send reset email?</h2><p>Firebase will send a secure password-reset link to this PG user. Their current role and permissions are unchanged.</p><div class="force-user-summary"><span class="user-avatar">{{userInitials(resetTarget()!)}}</span><div><b>{{resetTarget()!.name}}</b><small>{{resetTarget()!.email}}</small></div></div><div class="force-user-actions"><button class="secondary-btn" type="button" (click)="resetTarget.set(null)">Cancel</button><button class="primary-btn" type="button" [disabled]="resetBusy()" (click)="confirmResetPassword()">{{resetBusy()?'Sending…':'Send reset email'}}</button></div></section></div>}

`,styles:[`
.profile-access-hero{display:grid;grid-template-columns:1.2fr .8fr;gap:14px;margin-bottom:16px;background:linear-gradient(135deg,#fff 0%,#f7f4ff 48%,#effbff 100%)}.identity-card{display:flex;gap:14px;align-items:center}.profile-orb,.user-avatar,.tenant-mark{display:grid;place-items:center;border-radius:18px;background:linear-gradient(135deg,#6757f7,#14b8a6);color:#fff;font-weight:950}.profile-orb{width:68px;height:68px;font-size:22px}.identity-card h2{margin:2px 0;font-size:24px}.identity-card>div>span{color:var(--muted);font-size:11px}.identity-chips{display:flex;gap:6px;flex-wrap:wrap;margin-top:9px}.identity-chips b,.identity-chips em{padding:5px 8px;border-radius:999px;font-size:8px;font-style:normal;background:#f0edff;color:#574ac8}.identity-chips em{background:#eff9f6;color:#087a5c}.vault-card{border:1px solid #dcefe9;border-radius:17px;padding:15px;background:linear-gradient(145deg,#fff,#effbf7)}.vault-card span,.vault-card b,.vault-card small{display:block}.vault-card span{font-size:8px;letter-spacing:.1em;font-weight:950;color:#0b8967}.vault-card b{margin:5px 0;font-size:18px}.vault-card small{color:#617187;font-size:10px;line-height:1.5}.tenant-admin-panel,.user-admin-panel,.change-panel{margin-bottom:16px}.tenant-grid,.managed-user-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(245px,1fr));gap:9px}.tenant-grid article{display:grid;grid-template-columns:auto 1fr auto;gap:10px;align-items:center;padding:13px;border:1px solid #e4e8f0;border-radius:16px;background:#fff}.tenant-grid article.current{border-color:#786cf5;box-shadow:0 8px 25px rgba(103,87,247,.1)}.tenant-mark{width:42px;height:42px;border-radius:13px}.tenant-grid b,.tenant-grid small{display:block}.tenant-grid small{font-size:8px;color:#718096}.tenant-grid span{font-size:8px;font-weight:900;color:#5e51cf}.tenant-grid button{grid-column:2/4}.managed-user-card{padding:13px;border:1px solid #e4e8f0;border-radius:16px;background:linear-gradient(145deg,#fff,#fafbff)}.managed-user-card.disabled{opacity:.62}.managed-user-top{display:grid;grid-template-columns:auto 1fr auto;gap:9px;align-items:center}.user-avatar{width:40px;height:40px;border-radius:12px;font-size:11px}.managed-user-top b,.managed-user-top small{display:block}.managed-user-top small{font-size:9px;color:#7b8798}.managed-user-top em{font-size:8px;font-weight:950;text-transform:uppercase;font-style:normal;padding:4px 7px;border-radius:999px;background:#eef7ff;color:#1477a6}.managed-user-top em.owner{background:#f2edff;color:#6553cf}.user-meta{display:flex;justify-content:space-between;margin:11px 0 9px}.user-meta span{font-size:8px;color:#718096;background:#f6f8fb;border-radius:8px;padding:5px 7px}.compact{padding:8px 10px!important;font-size:9px}.audit-cards{display:grid;gap:7px}.audit-cards article{display:grid;grid-template-columns:auto 1fr;gap:10px;align-items:center;padding:10px;border:1px solid #e8eaf1;border-radius:12px}.audit-role{font-size:7px;font-weight:950;padding:5px 7px;border-radius:999px;background:#eaf7ff;color:#0b7fab}.audit-role.owner{background:#f0edff;color:#6050d0}.audit-cards b,.audit-cards small{display:block}.audit-cards small{font-size:9px;color:#7a8698}.access-user-modal{width:min(900px,calc(100vw - 24px))}.access-active{display:flex!important;align-items:center;gap:8px;margin-top:25px}.access-active input{width:auto!important}.access-permissions{margin-top:14px;padding-top:14px;border-top:1px solid #eaedf3}.owner-access-note{margin-top:14px;padding:13px;border:1px solid #dfd9ff;border-radius:13px;background:linear-gradient(135deg,#f7f4ff,#f3fbff)}.owner-access-note b,.owner-access-note span{display:block}.owner-access-note span{font-size:9px;color:#748095;margin-top:3px}.current-access-panel{margin-bottom:16px}.access-facts{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.access-facts article{padding:12px;border:1px solid #e6e9f0;border-radius:13px;background:#fafbff}.access-facts b,.access-facts small{display:block}.access-facts b{font-size:12px}.access-facts small{font-size:8px;color:#7c8798;margin-top:3px}.super-owner-entry{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:16px;margin-bottom:16px;background:radial-gradient(circle at 92% 10%,rgba(94,82,235,.10),transparent 32%),linear-gradient(135deg,#ffffff,#f8f8ff 55%,#f2fbf9);border:1px solid #e1e4f0;box-shadow:0 10px 30px rgba(42,53,92,.06)}.super-owner-icon{display:grid;place-items:center;width:58px;height:58px;border-radius:18px;background:linear-gradient(135deg,#6557f2,#13a99c);color:#fff;font-size:16px;font-weight:1000;box-shadow:0 12px 28px rgba(86,76,215,.22)}.super-owner-copy h2{margin:3px 0 5px;color:#171c30}.super-owner-copy p{max-width:760px;color:#657186;font-size:10px;line-height:1.55}.super-owner-pills{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}.super-owner-pills span{padding:5px 8px;border-radius:999px;background:#f1efff;color:#5e50ca;font-size:7px;font-weight:900}.super-owner-open-btn{display:inline-flex;align-items:center;gap:8px;white-space:nowrap;text-decoration:none;padding:11px 14px;border-radius:12px;background:linear-gradient(135deg,#5f52e9,#4a44cd);color:#fff;font-size:10px;font-weight:900;box-shadow:0 10px 22px rgba(82,70,207,.18)}.super-owner-open-btn span{font-size:14px}.account-flow{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:0 0 12px}.account-flow.four-step{grid-template-columns:repeat(4,1fr)}.account-flow article{padding:10px 12px;border:1px solid #e6e9f0;border-radius:12px;background:#fafbff}.account-flow b,.account-flow span{display:block}.account-flow b{font-size:10px}.account-flow span{font-size:8px;color:#788397;margin-top:2px}.owner-verify-box{display:grid;grid-template-columns:1fr 1fr;gap:12px;align-items:end;margin-top:14px;padding:13px;border:1px solid #e2e5ee;border-radius:14px;background:linear-gradient(135deg,#fbfbff,#f5fbf9)}.owner-verify-box>div b,.owner-verify-box>div span{display:block}.owner-verify-box>div b{font-size:11px}.owner-verify-box>div span{font-size:8px;color:#748095;margin-top:3px;line-height:1.45}.owner-verify-box label{font-size:8px;font-weight:900}.verify-password-field{position:relative;margin-top:5px}.verify-password-field input{width:100%;padding-right:58px}.verify-password-field button{position:absolute;right:5px;top:5px;height:34px;padding:0 9px;border:0;border-radius:8px;background:#ecebf9;color:#5549c8;font-size:8px;font-weight:900;cursor:pointer}.push-access-panel{background:linear-gradient(145deg,#fff,#f8fbff)}.push-access-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.push-access-grid article{padding:12px;border:1px solid #e5e9f1;border-radius:14px;background:#fbfcff}.push-access-grid b,.push-access-grid small{display:block}.push-access-grid b{font-size:10px;color:#1d2740}.push-access-grid small{font-size:8px;color:#748096;line-height:1.45;margin-top:4px}.push-actions{display:flex;gap:8px;margin-top:12px}.push-enabled{background:#e8faf3!important;color:#08765a!important}@media(max-width:900px){.push-access-grid{grid-template-columns:1fr 1fr}}@media(max-width:560px){.push-access-grid{grid-template-columns:1fr}.push-actions{display:grid}.push-actions button{width:100%}}.user-card-actions{display:flex;gap:7px;flex-wrap:wrap}.force-logout-user{color:#a74646!important;background:#fff8f8!important;border-color:#efdede!important}.force-user-logout-modal{position:relative;width:min(470px,calc(100vw - 24px));padding:26px;border-radius:24px;background:linear-gradient(155deg,#fff,#f8f9ff);box-shadow:0 28px 80px rgba(8,14,35,.28);border:1px solid #fff}.force-user-icon{display:grid;place-items:center;width:58px;height:58px;margin:0 auto 10px;border-radius:18px;background:linear-gradient(135deg,#fff0f0,#f0efff);color:#a84545;font-size:24px;font-weight:900}.force-user-logout-modal{text-align:center}.force-user-logout-modal h2{font-size:23px;margin:5px 0 7px}.force-user-logout-modal>p:not(.eyebrow){font-size:10px;color:#6e7a8e;line-height:1.55}.force-user-summary{display:flex;align-items:center;gap:10px;margin:15px 0;padding:10px;border:1px solid #e5e8ef;border-radius:13px;background:#fafbfe;text-align:left}.force-user-summary b,.force-user-summary small{display:block}.force-user-summary small{font-size:8px;color:#7a8698}.force-user-logout-modal>label{display:block;text-align:left;font-size:8px;font-weight:900}.force-user-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px}.danger-user-btn{border:0;border-radius:11px;background:linear-gradient(135deg,#c94d4d,#a53535);color:#fff;font-weight:900;cursor:pointer}.danger-user-btn:disabled{opacity:.6}.user-card-actions button{flex:1}@media(max-width:760px){.super-owner-entry{grid-template-columns:1fr;align-items:flex-start}.super-owner-entry .primary-btn{width:100%;text-align:center}.account-flow,.account-flow.four-step{grid-template-columns:1fr}.owner-verify-box{grid-template-columns:1fr}.access-facts{grid-template-columns:1fr 1fr}}@media(max-width:760px){.profile-access-hero{grid-template-columns:1fr}.permission-grid{grid-template-columns:1fr}}
.pg-identity-panel{margin-bottom:16px}.pg-identity-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:12px}.premium-field-card{display:grid;gap:6px;padding:14px;border:1px solid #e3e8f2;border-radius:16px;background:linear-gradient(145deg,#fff,#fafcff);min-width:0}.premium-field-card>span{font-size:10px;font-weight:950;color:#253047}.premium-field-card>small{font-size:8.5px;line-height:1.45;color:#7c8799}.premium-field-card input{width:100%;min-width:0}.premium-field-card.span-2{grid-column:1/-1}.pg-identity-actions{display:flex;justify-content:flex-end;gap:9px;margin-top:12px}@media(max-width:700px){.pg-identity-grid{grid-template-columns:1fr}.premium-field-card.span-2{grid-column:auto}.pg-identity-actions{display:grid;grid-template-columns:1fr 1fr}.pg-identity-actions button{width:100%}}
.sharing-rate-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:10px;margin-top:12px}.sharing-rate-grid label{padding:13px;border:1px solid #e5e8f1;border-radius:14px;background:linear-gradient(145deg,#fff,#fafbff);display:grid;gap:6px}.sharing-rate-grid label>span{font-size:10px;font-weight:900}.sharing-rate-grid label>small{font-size:8px;color:#7a8597}.rate-input{display:flex;align-items:center;border:1px solid #dde3ef;border-radius:10px;background:#fff;overflow:hidden}.rate-input b{padding:0 0 0 10px;color:#5b5fd6}.rate-input input{border:0!important;box-shadow:none!important;width:100%}.sharing-rate-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:12px}
`]})
export class ProfilePage implements OnInit{
 readonly config=APP_CONFIG;users=signal<ManagedUser[]>([]);loading=signal(false);error=signal('');message=signal('');editorOpen=signal(false);editorError=signal('');saving=signal(false);showVerificationPassword=signal(false);showTemporaryPassword=signal(false);verificationPassword='';editingUid='';forceLogoutTarget=signal<ManagedUser|null>(null);forceLogoutPassword='';showForceLogoutPassword=signal(false);forceLogoutBusy=signal(false);forceLogoutError=signal('');ownResetBusy=signal(false);resetTarget=signal<ManagedUser|null>(null);resetBusy=signal(false);
 readonly recommendedPermissions:PagePermission[]=['dashboard','property','residents','vacancy','payments','food','staff','maintenance','assets','utilities','vendors','calendar','documents','notifications'];
 draft:{name:string;email:string;password:string;role:Role;active:boolean;permissions:PagePermission[]}={name:'',email:'',password:'',role:'manager',active:true,permissions:[...this.recommendedPermissions]};
 permissions:{key:PagePermission,label:string,desc:string}[]=[{key:'dashboard',label:'Dashboard',desc:'Operational overview.'},{key:'property',label:'Property Map',desc:'View blocks, floors, rooms and beds.'},{key:'residents',label:'Residents',desc:'Resident lifecycle.'},{key:'vacancy',label:'Vacancy',desc:'Bed availability.'},{key:'payments',label:'Payments',desc:'Rent and receipts.'},{key:'food',label:'Food',desc:'Kitchen operations.'},{key:'staff',label:'Staff',desc:'Staff records.'},{key:'maintenance',label:'Maintenance',desc:'Repairs.'},{key:'assets',label:'Assets',desc:'Inventory.'},{key:'utilities',label:'Utilities',desc:'Utility operations.'},{key:'expenses',label:'Expenses',desc:'Expense ledger.'},{key:'vendors',label:'Vendors',desc:'Purchases.'},{key:'calendar',label:'Calendar',desc:'Schedules.'},{key:'reports',label:'Reports',desc:'Reports/exports.'},{key:'documents',label:'Documents',desc:'Document center.'},{key:'notifications',label:'Notifications',desc:'Alerts.'},{key:'activity',label:'Activity',desc:'Audit history.'}];
 pushBusy=signal(false);pgIdentity={name:'',shortName:'',city:''};pgIdentitySaving=signal(false);pgIdentityMessage=signal('');pgIdentityError=signal('');sharingRateDraft:{sharing:number;amount:number}[]=[];sharingRatesSaving=signal(false);sharingRatesMessage=signal('');sharingRatesError=signal('');
 constructor(public auth:AuthService,public store:StoreService,public notifications:NotificationService){}
 ngOnInit(){void this.refresh();void this.notifications.refreshSupportState();this.resetPgIdentity();this.refreshSharingRateDraft();}
 get initials(){return(this.auth.user()?.name||'PG').split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase();}userInitials(u:ManagedUser){return u.name.split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase();}formatTime(v:string){const d=new Date(v);return Number.isNaN(d.getTime())?v:d.toLocaleString();}

 resetPgIdentity(){const t=this.auth.tenant();this.pgIdentity={name:t?.name||this.config.property.name,shortName:t?.shortName||this.config.property.shortName,city:t?.city||this.config.property.city};this.pgIdentityMessage.set('');this.pgIdentityError.set('');}
 async savePgIdentity(){if(!this.auth.isOwner()||this.pgIdentitySaving())return;const name=this.pgIdentity.name.trim(),shortName=this.pgIdentity.shortName.trim(),city=this.pgIdentity.city.trim();this.pgIdentityError.set('');this.pgIdentityMessage.set('');if(name.length<2){this.pgIdentityError.set('Enter a valid PG name.');return;}if(shortName.length<2){this.pgIdentityError.set('Enter a short display name.');return;}if(city.length<2){this.pgIdentityError.set('Enter the PG location or city.');return;}this.pgIdentitySaving.set(true);try{await this.auth.updateTenantBranding({name,shortName,city});await this.store.auditAction('PG identity updated','Profile & Access',{name,shortName,city});this.pgIdentity={name,shortName,city};this.pgIdentityMessage.set('PG name and location saved to Firebase.');}catch(e:any){this.pgIdentityError.set(e?.message||'Unable to save PG name and location.');}finally{this.pgIdentitySaving.set(false);}}

 refreshSharingRateDraft(){const types:number[]=[...new Set<number>(this.store.rooms().map(r=>Number(r.sharing)))].sort((a,b)=>a-b);this.sharingRateDraft=types.map(sharing=>({sharing,amount:this.store.sharingRate(sharing)}));this.sharingRatesMessage.set('');this.sharingRatesError.set('');}
 sharingVacancyCount(sharing:number){return this.store.vacantBeds().filter(v=>v.room.sharing===sharing).length;}
 async saveSharingRates(){if(!this.auth.isOwner()||this.sharingRatesSaving())return;this.sharingRatesSaving.set(true);this.sharingRatesError.set('');this.sharingRatesMessage.set('');try{await this.store.saveSharingRates(this.sharingRateDraft);this.sharingRatesMessage.set('Sharing starting rates saved and synced to Firebase.');}catch(e:any){this.sharingRatesError.set(e?.message||'Unable to save sharing rates.');}finally{this.sharingRatesSaving.set(false);}}

 get pushLabel(){const s=this.notifications.state();return s==='enabled'?'Enabled':s==='blocked'?'Blocked':s==='not-configured'?'Setup Needed':s==='unsupported'?'Unsupported':'Available';}
 async enablePush(){if(this.pushBusy())return;this.pushBusy.set(true);try{await this.notifications.enablePush();}finally{this.pushBusy.set(false);}}
 async refresh(){this.loading.set(true);this.error.set('');try{if(this.auth.isOwner())this.users.set(await this.auth.listManagedUsers());}catch(e:any){this.error.set(e?.message||'Unable to load access data.');}finally{this.loading.set(false);}}
 openCreateUser(){this.editingUid='';this.editorError.set('');this.verificationPassword='';this.showVerificationPassword.set(false);this.showTemporaryPassword.set(false);this.draft={name:'',email:'',password:'',role:'manager',active:true,permissions:[...this.store.managerAccess()].filter(p=>!['amenities','settings'].includes(p))};this.editorOpen.set(true);}openEdit(u:ManagedUser){this.editingUid=u.uid;this.editorError.set('');this.verificationPassword='';this.showVerificationPassword.set(false);this.showTemporaryPassword.set(false);this.draft={name:u.name,email:u.email,password:'',role:u.role,active:u.active,permissions:[...u.permissions]};this.editorOpen.set(true);}closeEditor(){if(!this.saving()){this.editorOpen.set(false);this.verificationPassword='';this.showVerificationPassword.set(false);this.showTemporaryPassword.set(false);}}roleChanged(role:Role){if(role==='manager'&&!this.draft.permissions.length)this.draft.permissions=[...this.recommendedPermissions];}recommended(){this.draft.permissions=[...this.store.managerAccess()].filter(p=>!['amenities','settings'].includes(p));}togglePermission(p:PagePermission,on:boolean){this.draft.permissions=on?[...new Set([...this.draft.permissions,p])]:this.draft.permissions.filter(x=>x!==p);}
 async saveUser(){this.editorError.set('');if(!this.draft.name.trim()||!this.draft.email.trim()){this.editorError.set('Name and email are required.');return;}if(!this.editingUid&&this.draft.password.length<8){this.editorError.set('Temporary password must be at least 8 characters.');return;}if(!this.verificationPassword){this.editorError.set('Enter your current Owner password to confirm this access change.');return;}this.saving.set(true);try{await this.auth.verifyCurrentPassword(this.verificationPassword);if(this.editingUid){const current=this.users().find(x=>x.uid===this.editingUid);if(!current)throw new Error('User no longer exists.');const next:ManagedUser={...current,name:this.draft.name.trim(),role:this.draft.role,active:this.draft.active,permissions:this.draft.role==='owner'?[]:[...this.draft.permissions]};await this.auth.updateManagedUser(next);await this.store.auditAction('User access updated','Profile & Access',{targetUid:next.uid,targetName:next.name,role:next.role,active:next.active});this.message.set(`Access updated for ${next.name}.`);}else{const created=await this.auth.createManagedUser({name:this.draft.name.trim(),email:this.draft.email,password:this.draft.password,role:this.draft.role,permissions:this.draft.permissions});await this.store.auditAction('User created','Profile & Access',{targetUid:created.uid,targetName:created.name,role:created.role});this.message.set(`${created.name} created for ${this.auth.tenant()?.name}.`);}this.editorOpen.set(false);this.verificationPassword='';this.showVerificationPassword.set(false);await this.refresh();}catch(e:any){this.editorError.set(e?.message||'Unable to save user.');}finally{this.saving.set(false);}}
 openForceLogout(u:ManagedUser){this.forceLogoutTarget.set(u);this.forceLogoutPassword='';this.showForceLogoutPassword.set(false);this.forceLogoutError.set('');}
 closeForceLogout(){if(!this.forceLogoutBusy()){this.forceLogoutTarget.set(null);this.forceLogoutPassword='';this.showForceLogoutPassword.set(false);this.forceLogoutError.set('');}}
 async confirmForceLogout(){const u=this.forceLogoutTarget();if(!u)return;this.forceLogoutError.set('');if(!this.forceLogoutPassword){this.forceLogoutError.set('Enter your current Owner password.');return;}this.forceLogoutBusy.set(true);try{await this.auth.verifyCurrentPassword(this.forceLogoutPassword);await this.auth.forceManagedUserLogout(u.uid);await this.store.auditAction('User forced logout','Profile & Access',{targetUid:u.uid,targetName:u.name,role:u.role});this.message.set(`${u.name} was securely signed out.`);this.closeForceLogout();}catch(e:any){this.forceLogoutError.set(e?.message||'Unable to force logout this user.');}finally{this.forceLogoutBusy.set(false);}}
 managerVisiblePermissions(){const allowed=new Set(this.auth.user()?.permissions||[]);return this.permissions.filter(p=>allowed.has(p.key));}
 async sendOwnPasswordReset(){if(this.ownResetBusy())return;this.error.set('');this.ownResetBusy.set(true);try{await this.auth.sendOwnPasswordReset();this.message.set(`Password reset email sent to ${this.auth.user()?.email}.`);}catch(e:any){this.error.set(e?.message||'Unable to send password reset email.');}finally{this.ownResetBusy.set(false);}}
 resetPassword(u:ManagedUser){this.error.set('');this.resetTarget.set(u);}
 async confirmResetPassword(){const u=this.resetTarget();if(!u||this.resetBusy())return;this.resetBusy.set(true);this.error.set('');try{await this.auth.sendManagedUserPasswordReset(u.email);await this.store.auditAction('Password reset email sent','Profile & Access',{targetUid:u.uid,targetName:u.name});this.message.set(`Password reset email sent to ${u.email}.`);this.resetTarget.set(null);}catch(e:any){this.error.set(e?.message||'Unable to send password reset email.');}finally{this.resetBusy.set(false);}}

}